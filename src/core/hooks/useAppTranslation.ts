/**
 * Re-export of react-i18next `useTranslation` — the canonical `t(key)` API for the app.
 * Use with keys from `locales/en.json` / `locales/es.json` (also re-exported via `locales/en.ts`).
 */
export { useTranslation } from 'react-i18next'
