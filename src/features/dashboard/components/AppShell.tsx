/**
 * Re-export — canonical App shell lives in `src/router/layouts/AppShell.tsx`.
 * Import from here if your path alias expects `features/dashboard/components/AppShell`.
 */
export { AppShell, default } from '@/router/layouts/AppShell'
